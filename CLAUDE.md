Web 
- Never run 'npm run dev'
- Use 'npm run built' to check if code compiles or not. See results and fix code if it's neededs
Mobile App 
- Never run 'npx expo start' or 'npm run dev'
- Use 'npm run built' to check if code compiles or not.
- See results and fix code if needed.